name: Build macOS

on:
  push:
    branches: [main]
  pull_request:

env:
  BUILD_TEST_APP: true
  OPENXR_VERSION: "1.1.43"

jobs:
  build:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install dependencies
        run: |
          brew install cmake ninja eigen
          brew install glslang molten-vk vulkan-headers vulkan-loader
          # Set VULKAN_SDK to Homebrew prefix for CMake to find Vulkan
          BREW_PREFIX=$(brew --prefix)
          echo "VULKAN_SDK=$BREW_PREFIX" >> $GITHUB_ENV
          echo "Vulkan components installed via Homebrew at $BREW_PREFIX"
          which glslangValidator
          ls -la $BREW_PREFIX/lib/libvulkan* 2>/dev/null || true
          ls -la $BREW_PREFIX/lib/libMoltenVK* 2>/dev/null || true

      - name: Configure
        run: |
          cmake -B build -G Ninja \
            -DCMAKE_BUILD_TYPE=RelWithDebInfo \
            -DXRT_FEATURE_SERVICE=ON \
            -DXRT_FEATURE_HYBRID_MODE=ON

      - name: Build
        run: cmake --build build

      - name: Package runtime
        run: |
          mkdir -p _package/SRMonado-macOS/lib
          mkdir -p _package/SRMonado-macOS/share/vulkan/icd.d
          # Find and copy runtime module (.so on macOS for MODULE libraries)
          RUNTIME_LIB=$(find build/src/xrt/targets/openxr -name "libopenxr_monado*" -type f | head -1)
          cp "$RUNTIME_LIB" _package/SRMonado-macOS/lib/
          RUNTIME_BASENAME=$(basename "$RUNTIME_LIB")
          # Bundle Vulkan loader and MoltenVK from SDK (search multiple possible locations)
          for lib in libvulkan.1.dylib libMoltenVK.dylib; do
            if [ -f "$VULKAN_SDK/lib/$lib" ]; then
              cp "$VULKAN_SDK/lib/$lib" _package/SRMonado-macOS/lib/
            elif [ -f "$VULKAN_SDK/../lib/$lib" ]; then
              cp "$VULKAN_SDK/../lib/$lib" _package/SRMonado-macOS/lib/
            else
              FOUND=$(find "$VULKAN_SDK" -name "$lib" -type f 2>/dev/null | head -1)
              if [ -n "$FOUND" ]; then
                cp "$FOUND" _package/SRMonado-macOS/lib/
              else
                echo "Warning: $lib not found in SDK, skipping"
              fi
            fi
          done
          # Create MoltenVK ICD manifest pointing to bundled lib
          cat > _package/SRMonado-macOS/share/vulkan/icd.d/MoltenVK_icd.json <<EOF
          {
            "file_format_version": "1.0.0",
            "ICD": {
              "library_path": "../../../lib/libMoltenVK.dylib",
              "api_version": "1.2.0",
              "is_portability_driver": true
            }
          }
          EOF
          # Fix rpaths on the runtime so it finds bundled libvulkan
          install_name_tool -add_rpath @loader_path "$RUNTIME_LIB" 2>/dev/null || true
          install_name_tool -add_rpath @loader_path _package/SRMonado-macOS/lib/"$RUNTIME_BASENAME" 2>/dev/null || true
          # Generate portable manifest with relative library path
          cat > _package/SRMonado-macOS/openxr_monado.json <<EOF
          {
            "file_format_version": "1.0.0",
            "runtime": {
              "name": "Monado (SRMonado macOS)",
              "library_path": "lib/$RUNTIME_BASENAME"
            }
          }
          EOF

      - name: Build OpenXR loader
        if: env.BUILD_TEST_APP == 'true'
        run: |
          git clone --depth 1 --branch release-${{ env.OPENXR_VERSION }} \
            https://github.com/KhronosGroup/OpenXR-SDK-Source.git /tmp/openxr-sdk
          cmake -B /tmp/openxr-sdk/build -S /tmp/openxr-sdk -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_INSTALL_PREFIX=/tmp/openxr-install \
            -DBUILD_TESTS=OFF -DBUILD_CONFORMANCE_TESTS=OFF
          cmake --build /tmp/openxr-sdk/build
          cmake --install /tmp/openxr-sdk/build

      - name: Build test app (cube_vk_macos)
        if: env.BUILD_TEST_APP == 'true'
        run: |
          cmake -B test_apps/cube_vk_macos/build \
            -S test_apps/cube_vk_macos -G Ninja \
            -DCMAKE_BUILD_TYPE=RelWithDebInfo \
            -DCMAKE_PREFIX_PATH=/tmp/openxr-install
          cmake --build test_apps/cube_vk_macos/build

      - name: Build test app (cube_ext_vk_macos)
        if: env.BUILD_TEST_APP == 'true'
        run: |
          cmake -B test_apps/cube_ext_vk_macos/build \
            -S test_apps/cube_ext_vk_macos -G Ninja \
            -DCMAKE_BUILD_TYPE=RelWithDebInfo \
            -DCMAKE_PREFIX_PATH=/tmp/openxr-install
          cmake --build test_apps/cube_ext_vk_macos/build

      - name: Package test apps
        if: env.BUILD_TEST_APP == 'true'
        run: |
          mkdir -p _package/SRMonado-macOS/bin
          cp test_apps/cube_vk_macos/build/cube_vk_macos _package/SRMonado-macOS/bin/
          cp test_apps/cube_ext_vk_macos/build/cube_ext_vk_macos _package/SRMonado-macOS/bin/
          # Bundle the OpenXR loader dylib so it works without system install
          cp /tmp/openxr-install/lib/libopenxr_loader*.dylib _package/SRMonado-macOS/lib/
          # Fix rpaths so test apps and loader find bundled libvulkan/MoltenVK
          install_name_tool -add_rpath @executable_path/../lib _package/SRMonado-macOS/bin/cube_vk_macos 2>/dev/null || true
          install_name_tool -add_rpath @executable_path/../lib _package/SRMonado-macOS/bin/cube_ext_vk_macos 2>/dev/null || true
          install_name_tool -add_rpath @loader_path _package/SRMonado-macOS/lib/libopenxr_loader*.dylib 2>/dev/null || true
          # Create run script for cube_vk_macos
          cat > _package/SRMonado-macOS/run_cube_vk.sh <<'SCRIPT'
          #!/bin/bash
          DIR="$(cd "$(dirname "$0")" && pwd)"
          export XR_RUNTIME_JSON="$DIR/openxr_monado.json"
          export DYLD_LIBRARY_PATH="$DIR/lib:${DYLD_LIBRARY_PATH:-}"
          export VK_ICD_FILENAMES="$DIR/share/vulkan/icd.d/MoltenVK_icd.json"
          export SIM_DISPLAY_ENABLE=1
          export SIM_DISPLAY_OUTPUT="${SIM_DISPLAY_OUTPUT:-anaglyph}"
          echo "Starting cube_vk_macos with $SIM_DISPLAY_OUTPUT output..."
          exec "$DIR/bin/cube_vk_macos" "$@"
          SCRIPT
          chmod +x _package/SRMonado-macOS/run_cube_vk.sh

      - name: Build 3DGS demo app (gaussian_splatting_vk_macos)
        if: env.BUILD_TEST_APP == 'true'
        run: |
          cmake -B demos/gaussian_splatting_vk_macos/build \
            -S demos/gaussian_splatting_vk_macos -G Ninja \
            -DCMAKE_BUILD_TYPE=RelWithDebInfo \
            -DCMAKE_PREFIX_PATH=/tmp/openxr-install
          cmake --build demos/gaussian_splatting_vk_macos/build

      - name: Package 3DGS demo app
        if: env.BUILD_TEST_APP == 'true'
        run: |
          cp demos/gaussian_splatting_vk_macos/build/gaussian_splatting_vk_macos _package/SRMonado-macOS/bin/
          install_name_tool -add_rpath @executable_path/../lib _package/SRMonado-macOS/bin/gaussian_splatting_vk_macos 2>/dev/null || true
          # Create run script
          cat > _package/SRMonado-macOS/run_gaussian_splatting.sh <<'SCRIPT'
          #!/bin/bash
          DIR="$(cd "$(dirname "$0")" && pwd)"
          export XR_RUNTIME_JSON="$DIR/openxr_monado.json"
          export DYLD_LIBRARY_PATH="$DIR/lib:${DYLD_LIBRARY_PATH:-}"
          export VK_ICD_FILENAMES="$DIR/share/vulkan/icd.d/MoltenVK_icd.json"
          export SIM_DISPLAY_ENABLE=1
          export SIM_DISPLAY_OUTPUT="${SIM_DISPLAY_OUTPUT:-anaglyph}"
          echo "Starting gaussian_splatting_vk_macos (3D Gaussian Splatting) with $SIM_DISPLAY_OUTPUT output..."
          exec "$DIR/bin/gaussian_splatting_vk_macos" "$@"
          SCRIPT
          chmod +x _package/SRMonado-macOS/run_gaussian_splatting.sh

      - name: Build macOS installer (.pkg)
        run: |
          chmod +x installer/macos/build_installer.sh
          chmod +x installer/macos/create_app_bundle.sh
          ./installer/macos/build_installer.sh _package/SRMonado-macOS _package/SRMonado-Installer.pkg

      - name: Upload macOS artifact
        uses: actions/upload-artifact@v4
        with:
          name: SRMonado-macOS
          if-no-files-found: error
          path: |
            _package/SRMonado-macOS/
            _package/SRMonado-Installer.pkg
