name: Build macOS (DISABLED)

on:
  push:
    branches: [main, lightweight-runtime-ci]

# Temporarily disabled to save CI minutes — building locally instead
if: false
  pull_request:

env:
  BUILD_TEST_APP: true
  OPENXR_VERSION: "1.1.43"

jobs:
  build:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install dependencies
        run: |
          brew install cmake ninja eigen
          brew install glslang molten-vk vulkan-headers vulkan-loader
          # Set VULKAN_SDK to Homebrew prefix for CMake to find Vulkan
          BREW_PREFIX=$(brew --prefix)
          echo "VULKAN_SDK=$BREW_PREFIX" >> $GITHUB_ENV
          echo "Vulkan components installed via Homebrew at $BREW_PREFIX"
          which glslangValidator
          ls -la $BREW_PREFIX/lib/libvulkan* 2>/dev/null || true
          ls -la $BREW_PREFIX/lib/libMoltenVK* 2>/dev/null || true

      - name: Configure
        run: |
          cmake -B build -G Ninja \
            -DCMAKE_BUILD_TYPE=RelWithDebInfo \
            -DXRT_FEATURE_SERVICE=ON \
            -DXRT_FEATURE_HYBRID_MODE=ON

      - name: Build
        run: cmake --build build

      - name: Package runtime
        run: |
          mkdir -p _package/DisplayXR-macOS/lib
          mkdir -p _package/DisplayXR-macOS/share/vulkan/icd.d
          # Find and copy runtime module (.so on macOS for MODULE libraries)
          RUNTIME_LIB=$(find build/src/xrt/targets/openxr -name "libopenxr_displayxr*" -type f | head -1)
          cp "$RUNTIME_LIB" _package/DisplayXR-macOS/lib/
          RUNTIME_BASENAME=$(basename "$RUNTIME_LIB")
          # Bundle Vulkan loader and MoltenVK from SDK (search multiple possible locations)
          for lib in libvulkan.1.dylib libMoltenVK.dylib; do
            if [ -f "$VULKAN_SDK/lib/$lib" ]; then
              cp "$VULKAN_SDK/lib/$lib" _package/DisplayXR-macOS/lib/
            elif [ -f "$VULKAN_SDK/../lib/$lib" ]; then
              cp "$VULKAN_SDK/../lib/$lib" _package/DisplayXR-macOS/lib/
            else
              FOUND=$(find "$VULKAN_SDK" -name "$lib" -type f 2>/dev/null | head -1)
              if [ -n "$FOUND" ]; then
                cp "$FOUND" _package/DisplayXR-macOS/lib/
              else
                echo "Warning: $lib not found in SDK, skipping"
              fi
            fi
          done
          # Create MoltenVK ICD manifest pointing to bundled lib
          cat > _package/DisplayXR-macOS/share/vulkan/icd.d/MoltenVK_icd.json <<EOF
          {
            "file_format_version": "1.0.0",
            "ICD": {
              "library_path": "../../../lib/libMoltenVK.dylib",
              "api_version": "1.2.0",
              "is_portability_driver": true
            }
          }
          EOF
          # Fix rpaths on the runtime so it finds bundled libvulkan
          install_name_tool -add_rpath @loader_path "$RUNTIME_LIB" 2>/dev/null || true
          install_name_tool -add_rpath @loader_path _package/DisplayXR-macOS/lib/"$RUNTIME_BASENAME" 2>/dev/null || true
          # Generate portable manifest with relative library path
          cat > _package/DisplayXR-macOS/openxr_displayxr.json <<EOF
          {
            "file_format_version": "1.0.0",
            "runtime": {
              "name": "DisplayXR Runtime",
              "library_path": "lib/$RUNTIME_BASENAME"
            }
          }
          EOF

      - name: Build OpenXR loader
        if: env.BUILD_TEST_APP == 'true'
        run: |
          git clone --depth 1 --branch release-${{ env.OPENXR_VERSION }} \
            https://github.com/KhronosGroup/OpenXR-SDK-Source.git /tmp/openxr-sdk
          cmake -B /tmp/openxr-sdk/build -S /tmp/openxr-sdk -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_INSTALL_PREFIX=/tmp/openxr-install \
            -DBUILD_TESTS=OFF -DBUILD_CONFORMANCE_TESTS=OFF
          cmake --build /tmp/openxr-sdk/build
          cmake --install /tmp/openxr-sdk/build

      - name: Build test app (cube_handle_vk_macos)
        if: env.BUILD_TEST_APP == 'true'
        run: |
          cmake -B test_apps/cube_handle_vk_macos/build \
            -S test_apps/cube_handle_vk_macos -G Ninja \
            -DCMAKE_BUILD_TYPE=RelWithDebInfo \
            -DCMAKE_PREFIX_PATH=/tmp/openxr-install
          cmake --build test_apps/cube_handle_vk_macos/build

      - name: Build test app (cube_handle_metal_macos)
        if: env.BUILD_TEST_APP == 'true'
        run: |
          cmake -B test_apps/cube_handle_metal_macos/build \
            -S test_apps/cube_handle_metal_macos -G Ninja \
            -DCMAKE_BUILD_TYPE=RelWithDebInfo \
            -DCMAKE_PREFIX_PATH=/tmp/openxr-install
          cmake --build test_apps/cube_handle_metal_macos/build

      - name: Build test app (cube_handle_gl_macos)
        if: env.BUILD_TEST_APP == 'true'
        run: |
          cmake -B test_apps/cube_handle_gl_macos/build \
            -S test_apps/cube_handle_gl_macos -G Ninja \
            -DCMAKE_BUILD_TYPE=RelWithDebInfo \
            -DCMAKE_PREFIX_PATH=/tmp/openxr-install
          cmake --build test_apps/cube_handle_gl_macos/build

      - name: Build test app (cube_texture_metal_macos)
        if: env.BUILD_TEST_APP == 'true'
        run: |
          cmake -B test_apps/cube_texture_metal_macos/build \
            -S test_apps/cube_texture_metal_macos -G Ninja \
            -DCMAKE_BUILD_TYPE=RelWithDebInfo \
            -DCMAKE_PREFIX_PATH=/tmp/openxr-install
          cmake --build test_apps/cube_texture_metal_macos/build

      - name: Build test app (cube_hosted_metal_macos)
        if: env.BUILD_TEST_APP == 'true'
        run: |
          cmake -B test_apps/cube_hosted_metal_macos/build \
            -S test_apps/cube_hosted_metal_macos -G Ninja \
            -DCMAKE_BUILD_TYPE=RelWithDebInfo \
            -DCMAKE_PREFIX_PATH=/tmp/openxr-install
          cmake --build test_apps/cube_hosted_metal_macos/build

      - name: Build test app (cube_hosted_legacy_metal_macos)
        if: env.BUILD_TEST_APP == 'true'
        run: |
          cmake -B test_apps/cube_hosted_legacy_metal_macos/build \
            -S test_apps/cube_hosted_legacy_metal_macos -G Ninja \
            -DCMAKE_BUILD_TYPE=RelWithDebInfo \
            -DCMAKE_PREFIX_PATH=/tmp/openxr-install
          cmake --build test_apps/cube_hosted_legacy_metal_macos/build

      - name: Build test app (cube_hosted_legacy_gl_macos)
        if: env.BUILD_TEST_APP == 'true'
        run: |
          cmake -B test_apps/cube_hosted_legacy_gl_macos/build \
            -S test_apps/cube_hosted_legacy_gl_macos -G Ninja \
            -DCMAKE_BUILD_TYPE=RelWithDebInfo \
            -DCMAKE_PREFIX_PATH=/tmp/openxr-install
          cmake --build test_apps/cube_hosted_legacy_gl_macos/build

      - name: Build test app (cube_hosted_legacy_vk_macos)
        if: env.BUILD_TEST_APP == 'true'
        run: |
          cmake -B test_apps/cube_hosted_legacy_vk_macos/build \
            -S test_apps/cube_hosted_legacy_vk_macos -G Ninja \
            -DCMAKE_BUILD_TYPE=RelWithDebInfo \
            -DCMAKE_PREFIX_PATH=/tmp/openxr-install
          cmake --build test_apps/cube_hosted_legacy_vk_macos/build

      - name: Package test apps
        if: env.BUILD_TEST_APP == 'true'
        run: |
          mkdir -p _package/DisplayXR-macOS/bin
          cp test_apps/cube_handle_vk_macos/build/cube_handle_vk_macos _package/DisplayXR-macOS/bin/
          cp test_apps/cube_handle_metal_macos/build/cube_handle_metal_macos _package/DisplayXR-macOS/bin/
          cp test_apps/cube_handle_gl_macos/build/cube_handle_gl_macos _package/DisplayXR-macOS/bin/
          cp test_apps/cube_texture_metal_macos/build/cube_texture_metal_macos _package/DisplayXR-macOS/bin/
          cp test_apps/cube_hosted_metal_macos/build/cube_hosted_metal_macos _package/DisplayXR-macOS/bin/
          cp test_apps/cube_hosted_legacy_metal_macos/build/cube_hosted_legacy_metal_macos _package/DisplayXR-macOS/bin/
          cp test_apps/cube_hosted_legacy_gl_macos/build/cube_hosted_legacy_gl_macos _package/DisplayXR-macOS/bin/
          cp test_apps/cube_hosted_legacy_vk_macos/build/cube_hosted_legacy_vk_macos _package/DisplayXR-macOS/bin/
          # Bundle the OpenXR loader dylib so it works without system install
          cp /tmp/openxr-install/lib/libopenxr_loader*.dylib _package/DisplayXR-macOS/lib/
          # Fix rpaths so test apps and loader find bundled libvulkan/MoltenVK
          install_name_tool -add_rpath @executable_path/../lib _package/DisplayXR-macOS/bin/cube_handle_vk_macos 2>/dev/null || true
          install_name_tool -add_rpath @executable_path/../lib _package/DisplayXR-macOS/bin/cube_handle_metal_macos 2>/dev/null || true
          install_name_tool -add_rpath @executable_path/../lib _package/DisplayXR-macOS/bin/cube_handle_gl_macos 2>/dev/null || true
          install_name_tool -add_rpath @executable_path/../lib _package/DisplayXR-macOS/bin/cube_texture_metal_macos 2>/dev/null || true
          install_name_tool -add_rpath @executable_path/../lib _package/DisplayXR-macOS/bin/cube_hosted_metal_macos 2>/dev/null || true
          install_name_tool -add_rpath @executable_path/../lib _package/DisplayXR-macOS/bin/cube_hosted_legacy_metal_macos 2>/dev/null || true
          install_name_tool -add_rpath @executable_path/../lib _package/DisplayXR-macOS/bin/cube_hosted_legacy_gl_macos 2>/dev/null || true
          install_name_tool -add_rpath @executable_path/../lib _package/DisplayXR-macOS/bin/cube_hosted_legacy_vk_macos 2>/dev/null || true
          install_name_tool -add_rpath @loader_path _package/DisplayXR-macOS/lib/libopenxr_loader*.dylib 2>/dev/null || true
          # Copy textures for test apps
          mkdir -p _package/DisplayXR-macOS/bin/textures
          cp test_apps/common/textures/*.jpg _package/DisplayXR-macOS/bin/textures/ 2>/dev/null || true
          # Create run script for cube_handle_vk_macos
          cat > _package/DisplayXR-macOS/run_cube_handle_vk.sh <<'SCRIPT'
          #!/bin/bash
          DIR="$(cd "$(dirname "$0")" && pwd)"
          export XR_RUNTIME_JSON="$DIR/openxr_displayxr.json"
          export DYLD_LIBRARY_PATH="$DIR/lib:${DYLD_LIBRARY_PATH:-}"
          export VK_ICD_FILENAMES="$DIR/share/vulkan/icd.d/MoltenVK_icd.json"
          export SIM_DISPLAY_ENABLE=1
          export SIM_DISPLAY_OUTPUT="${SIM_DISPLAY_OUTPUT:-anaglyph}"
          echo "Starting cube_handle_vk_macos (Vulkan, window handle) with $SIM_DISPLAY_OUTPUT output..."
          exec "$DIR/bin/cube_handle_vk_macos" "$@"
          SCRIPT
          chmod +x _package/DisplayXR-macOS/run_cube_handle_vk.sh
          # Create run script for cube_handle_metal_macos (no Vulkan env vars needed)
          cat > _package/DisplayXR-macOS/run_cube_handle_metal.sh <<'SCRIPT'
          #!/bin/bash
          DIR="$(cd "$(dirname "$0")" && pwd)"
          export XR_RUNTIME_JSON="$DIR/openxr_displayxr.json"
          export DYLD_LIBRARY_PATH="$DIR/lib:${DYLD_LIBRARY_PATH:-}"
          export SIM_DISPLAY_ENABLE=1
          export SIM_DISPLAY_OUTPUT="${SIM_DISPLAY_OUTPUT:-anaglyph}"
          echo "Starting cube_handle_metal_macos (Metal, window handle) with $SIM_DISPLAY_OUTPUT output..."
          exec "$DIR/bin/cube_handle_metal_macos" "$@"
          SCRIPT
          chmod +x _package/DisplayXR-macOS/run_cube_handle_metal.sh
          # Create run script for cube_handle_gl_macos (no Vulkan env vars needed)
          cat > _package/DisplayXR-macOS/run_cube_handle_gl.sh <<'SCRIPT'
          #!/bin/bash
          DIR="$(cd "$(dirname "$0")" && pwd)"
          export XR_RUNTIME_JSON="$DIR/openxr_displayxr.json"
          export DYLD_LIBRARY_PATH="$DIR/lib:${DYLD_LIBRARY_PATH:-}"
          export SIM_DISPLAY_ENABLE=1
          export SIM_DISPLAY_OUTPUT="${SIM_DISPLAY_OUTPUT:-anaglyph}"
          echo "Starting cube_handle_gl_macos (OpenGL, window handle) with $SIM_DISPLAY_OUTPUT output..."
          exec "$DIR/bin/cube_handle_gl_macos" "$@"
          SCRIPT
          chmod +x _package/DisplayXR-macOS/run_cube_handle_gl.sh
          # Create run script for cube_texture_metal_macos (IOSurface shared texture)
          cat > _package/DisplayXR-macOS/run_cube_texture_metal.sh <<'SCRIPT'
          #!/bin/bash
          DIR="$(cd "$(dirname "$0")" && pwd)"
          export XR_RUNTIME_JSON="$DIR/openxr_displayxr.json"
          export DYLD_LIBRARY_PATH="$DIR/lib:${DYLD_LIBRARY_PATH:-}"
          export SIM_DISPLAY_ENABLE=1
          export SIM_DISPLAY_OUTPUT="${SIM_DISPLAY_OUTPUT:-anaglyph}"
          echo "Starting cube_texture_metal_macos (Metal, IOSurface shared texture) with $SIM_DISPLAY_OUTPUT output..."
          exec "$DIR/bin/cube_texture_metal_macos" "$@"
          SCRIPT
          chmod +x _package/DisplayXR-macOS/run_cube_texture_metal.sh
          # Create run script for cube_hosted_metal_macos
          cat > _package/DisplayXR-macOS/run_cube_hosted_metal.sh <<'SCRIPT'
          #!/bin/bash
          DIR="$(cd "$(dirname "$0")" && pwd)"
          export XR_RUNTIME_JSON="$DIR/openxr_displayxr.json"
          export DYLD_LIBRARY_PATH="$DIR/lib:${DYLD_LIBRARY_PATH:-}"
          export SIM_DISPLAY_ENABLE=1
          export SIM_DISPLAY_OUTPUT="${SIM_DISPLAY_OUTPUT:-anaglyph}"
          echo "Starting cube_hosted_metal_macos (Metal, hosted) with $SIM_DISPLAY_OUTPUT output..."
          exec "$DIR/bin/cube_hosted_metal_macos" "$@"
          SCRIPT
          chmod +x _package/DisplayXR-macOS/run_cube_hosted_metal.sh
          # Create run script for cube_hosted_legacy_metal_macos
          cat > _package/DisplayXR-macOS/run_cube_hosted_legacy_metal.sh <<'SCRIPT'
          #!/bin/bash
          DIR="$(cd "$(dirname "$0")" && pwd)"
          export XR_RUNTIME_JSON="$DIR/openxr_displayxr.json"
          export DYLD_LIBRARY_PATH="$DIR/lib:${DYLD_LIBRARY_PATH:-}"
          export SIM_DISPLAY_ENABLE=1
          export SIM_DISPLAY_OUTPUT="${SIM_DISPLAY_OUTPUT:-anaglyph}"
          echo "Starting cube_hosted_legacy_metal_macos (Metal, legacy hosted) with $SIM_DISPLAY_OUTPUT output..."
          exec "$DIR/bin/cube_hosted_legacy_metal_macos" "$@"
          SCRIPT
          chmod +x _package/DisplayXR-macOS/run_cube_hosted_legacy_metal.sh
          # Create run script for cube_hosted_legacy_gl_macos
          cat > _package/DisplayXR-macOS/run_cube_hosted_legacy_gl.sh <<'SCRIPT'
          #!/bin/bash
          DIR="$(cd "$(dirname "$0")" && pwd)"
          export XR_RUNTIME_JSON="$DIR/openxr_displayxr.json"
          export DYLD_LIBRARY_PATH="$DIR/lib:${DYLD_LIBRARY_PATH:-}"
          export SIM_DISPLAY_ENABLE=1
          export SIM_DISPLAY_OUTPUT="${SIM_DISPLAY_OUTPUT:-anaglyph}"
          echo "Starting cube_hosted_legacy_gl_macos (OpenGL, legacy hosted) with $SIM_DISPLAY_OUTPUT output..."
          exec "$DIR/bin/cube_hosted_legacy_gl_macos" "$@"
          SCRIPT
          chmod +x _package/DisplayXR-macOS/run_cube_hosted_legacy_gl.sh
          # Create run script for cube_hosted_legacy_vk_macos
          cat > _package/DisplayXR-macOS/run_cube_hosted_legacy_vk.sh <<'SCRIPT'
          #!/bin/bash
          DIR="$(cd "$(dirname "$0")" && pwd)"
          export XR_RUNTIME_JSON="$DIR/openxr_displayxr.json"
          export DYLD_LIBRARY_PATH="$DIR/lib:${DYLD_LIBRARY_PATH:-}"
          export VK_ICD_FILENAMES="$DIR/share/vulkan/icd.d/MoltenVK_icd.json"
          export SIM_DISPLAY_ENABLE=1
          export SIM_DISPLAY_OUTPUT="${SIM_DISPLAY_OUTPUT:-anaglyph}"
          echo "Starting cube_hosted_legacy_vk_macos (Vulkan, legacy hosted) with $SIM_DISPLAY_OUTPUT output..."
          exec "$DIR/bin/cube_hosted_legacy_vk_macos" "$@"
          SCRIPT
          chmod +x _package/DisplayXR-macOS/run_cube_hosted_legacy_vk.sh

      - name: Build 3DGS demo app (gaussian_splatting_handle_vk_macos)
        if: env.BUILD_TEST_APP == 'true'
        run: |
          cmake -B demos/gaussian_splatting_handle_vk_macos/build \
            -S demos/gaussian_splatting_handle_vk_macos -G Ninja \
            -DCMAKE_BUILD_TYPE=RelWithDebInfo \
            -DCMAKE_PREFIX_PATH=/tmp/openxr-install
          cmake --build demos/gaussian_splatting_handle_vk_macos/build

      - name: Package 3DGS demo app
        if: env.BUILD_TEST_APP == 'true'
        run: |
          cp demos/gaussian_splatting_handle_vk_macos/build/gaussian_splatting_handle_vk_macos _package/DisplayXR-macOS/bin/
          install_name_tool -add_rpath @executable_path/../lib _package/DisplayXR-macOS/bin/gaussian_splatting_handle_vk_macos 2>/dev/null || true
          # Create run script
          cat > _package/DisplayXR-macOS/run_gaussian_splatting_handle_vk.sh <<'SCRIPT'
          #!/bin/bash
          DIR="$(cd "$(dirname "$0")" && pwd)"
          export XR_RUNTIME_JSON="$DIR/openxr_displayxr.json"
          export DYLD_LIBRARY_PATH="$DIR/lib:${DYLD_LIBRARY_PATH:-}"
          export VK_ICD_FILENAMES="$DIR/share/vulkan/icd.d/MoltenVK_icd.json"
          export SIM_DISPLAY_ENABLE=1
          export SIM_DISPLAY_OUTPUT="${SIM_DISPLAY_OUTPUT:-anaglyph}"
          echo "Starting gaussian_splatting_handle_vk_macos (3D Gaussian Splatting) with $SIM_DISPLAY_OUTPUT output..."
          exec "$DIR/bin/gaussian_splatting_handle_vk_macos" "$@"
          SCRIPT
          chmod +x _package/DisplayXR-macOS/run_gaussian_splatting_handle_vk.sh

      - name: Build macOS installer (.pkg)
        run: |
          chmod +x installer/macos/build_installer.sh
          chmod +x installer/macos/create_app_bundle.sh
          ./installer/macos/build_installer.sh _package/DisplayXR-macOS _package/DisplayXR-Installer.pkg

      - name: Upload macOS artifact
        uses: actions/upload-artifact@v4
        with:
          name: DisplayXR-macOS
          if-no-files-found: error
          path: |
            _package/DisplayXR-macOS/
            _package/DisplayXR-Installer.pkg
